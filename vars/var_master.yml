---
master_ip: "{{ ansible_host }}"
master_hostname: "{{ ansible_hostname }}"
second_master_hostname": "{{ second_master_hostname }}"
worker_name: "{{ ansible_hostname }}"
current_ip: "{{ ansible_hostname }}"

